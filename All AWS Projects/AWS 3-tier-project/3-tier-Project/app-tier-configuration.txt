curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash

source ~/.bashrc

nvm install 16

nvm use 16

npm install -g pm2

cd ~/

aws s3 cp s3://project-code-3ri/app-tier/ app-tier --recursive   ----this is the path of s3 files

cd ~/app-tier

npm install

pm2 start index.js

pm2 list

pm2 startup

*copy above command output and execute it *

pm2 save

sudo curl http://localhost:4000/health

curl http://localhost:4000/transaction